// #include "../include/glw_mesh.h"

// glw_mesh::gl_mesh(){
// }

// glw_mesh::~gl_mesh(){
// }

// glw_mesh::loadMeshData(const vertices *mesh, const float *element_index, size_t mesh_n, size_t index_n, GLenum usage){
//     vbo.bind();
//     vbo.loadData(mesh, mesh_n, usage);
//     vao.bind();
//     vao.attribData(0, 3, GL_FLOAT, GL_FALSE, sizeof(vertices), (const void*)offsetof(struct vertices, position));
//     vao.attribData(1, 3, GL_FLOAT, GL_FALSE, sizeof(vertices), (const void*)offsetof(struct vertices, normal));
//     vao.attribData(2, 2, GL_FLOAT, GL_FALSE, sizeof(vertices), (const void*)offsetof(struct vertices, texture));
//     vao.eboData(element_index, index_n, usage)
// }

// glw_mesh::draw(gl_program &program, const gl_shader &fshader){
//     program.changeShader()
// }
